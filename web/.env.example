# URL de la API de F2. En dev, vite.config.ts la usa como proxy de /api
VITE_API_URL=http://localhost:8000

# true = trabaja contra mocks de MSW, sin necesidad de que F2 exista.
# Es el modo por defecto de desarrollo mientras F2 no esté listo.
VITE_USE_MOCKS=true
